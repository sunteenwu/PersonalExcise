<Page
    x:Class="CDataBinding.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDataBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="50">
        <StackPanel Orientation="Horizontal">
            <ComboBox Name="SetComboBox" ItemsSource="{x:Bind MagicSets}" Width="300">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="local:Set">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{x:Bind setImageUrl}"/>
                            <TextBlock Text="{x:Bind name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button x:Name="BtnGetimageuri" Click="BtnGetimageuri_Click" Content="Get Image Uri"></Button>
        </StackPanel>
    </Grid>
</Page>
